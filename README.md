# pacman + fzf

A simple script that combines `pacman -Si`, `pacman -Sl` and `pacman -S --needed` for convenient package installation and query with fzf via the cli.

![Interface](assets/pac-fzf.png)
![Installation](assets/pac-fzf-install.png)
